#include <iostream>
#include "bin_search_tree.hpp"

using namespace std;


// put binary tree initialization here (call ze nodes)
// variable = new BIN_node(value, nullptr, nullptr)
// 


// put functions here
void test_function(){
    cout << "hello, you did the test! (test_function() called)" << endl;
}

void in_order_traverse(){
    // pseudo below
    /* 
    if(current->left != nullptr) {
        doo stuff (call recursively with current->left passed in.)
        in_order_traverse(current->left)
    } 

    print the node value:
    cout << current << ", ";

    if(current->right != nullptr) {
        doo stuff (call recursively with current->right passed in.)
        in_order_traverse(current->right)
    } 
    */
}

void pre_order_traverse() {
    // code
}

void post_order_traverse() {
    // code
}